import { connect } from 'react-redux';
import AboutPage from './about_page';

const mSTP = (state, ownProps) => {return {}};

const mDTP = dispatch => ({});

export default connect(mSTP, mDTP)(AboutPage);